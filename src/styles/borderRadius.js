// 5-tier border radius system
const BORDER_RADIUS = {
  none: '0px',
  compact: '12px',     // Small elements (buttons, inputs)
  card: '20px',        // Cards, dialogs
  large: '28px',       // Large containers
  button: '12px',      // Specifically for buttons
  full: '9999px',      // Pills, circular elements
};

export default BORDER_RADIUS;
